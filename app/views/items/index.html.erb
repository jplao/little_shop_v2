<h2> Statistics </h2>
<div class= "statistics">
  <div class= "top_item">
    <h2> Top Items </h2>
    <ul>
    <% @top_item.each do |item| %>
      <li><%= item.name %></li>
    <% end %>
    </ul>
  </div>

  <div class= "top_merch">
    <h2> Top Merchants </h2>
    <ul>
    <% @top_merch.each do |merch| %>
      <li><%= merch.name %></li>
    <% end %>
    </ul>
  </div>

  <div class= "quick-merchants">
    Merchants Who Ship Fastest
    <% @quick_merch.each do |merch| %>
      <%= merch.name %>
    <% end %>
  </div>

  <div class= "slow-merchants">
    Merchants Who Ship Slowest
    <% @slow_merch.each do |merch| %>
      <%= merch.name %>
    <% end %>
  </div>
</div>
<br>

<h2>All Items</h2>

<div class='items'>
<% @items.each do |item| %>
  <div class='each_item' id="item<%=item.id%>">
    <%= link_to image_tag(item.image, alt: "image-#{item.id}"), item_path(item) %>
    <br><br><%= link_to item.name, item_path(item) %>
    <br><br>Merchant: <%= item.user.name %>
    <br>Inventory count: <%= item.inventory_count %>
    <br>Item price: $<%= item.price %>
  </div>
<% end %>
</div>
