<% @items.each do |item| %>
    <%= link_to item.name, item_path(item) %>
    <%= item.user.name %>
    <%= link_to image_tag(item.image, alt: "image-#{item.id}"), item_path(item) %>
    <%= item.inventory_count %>
    <%= item.price %>
<% end %>
